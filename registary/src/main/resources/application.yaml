spring:
  application:
    name: registry

server:
  port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
  server:
    enable-self-preservation: false  # Helps during development

# Disable all security if Spring Security is on classpath
spring.security.enabled: false
management.security.enabled: false